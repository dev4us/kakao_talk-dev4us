import React from "react";
import { NavLink } from "react-router-dom";

const SettingPresenter = () => (
  <div>
    <NavLink to="/test">Setting</NavLink>
  </div>
);

export default SettingPresenter;
